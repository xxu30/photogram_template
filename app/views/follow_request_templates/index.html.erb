<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All follow requests
    </h1>

    <a href="/follow_requests/new" class="btn btn-block btn-outline-secondary">
      Add a new follow request
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Sender
        </th>

        <th>
          Recipient
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @follow_requests.each do |follow_request| %>
      <tr>
        <!-- Display follow_request.id start -->
        <td>
          <%= follow_request.id %>
        </td>
        <!-- Display follow_request.id end -->

        <!-- Display follow_request.sender_id start -->
        <td>
          <%= follow_request.sender.username %>
        </td>
        <!-- Display follow_request.sender_id end -->

        <!-- Display follow_request.recipient_id start -->
        <td>
          <%= follow_request.recipient.username %>
        </td>
        <!-- Display follow_request.recipient_id end -->

        <!-- Display follow_request.created_at start -->
        <td>
          <%= time_ago_in_words(follow_request.created_at) %> ago
        </td>
        <!-- Display follow_request.created_at end -->

        <!-- Display follow_request.updated_at start -->
        <td>
          <%= time_ago_in_words(follow_request.updated_at) %> ago
        </td>
        <!-- Display follow_request.updated_at end -->

        <td>
          <a href="/follow_requests/<%= follow_request.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
